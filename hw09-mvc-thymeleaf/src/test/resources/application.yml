spring:
  datasource:
    url: jdbc:h2:mem:testdb
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.yaml
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    open-in-view: false